---
title: Maxkore的文档站 · 使用指南
description: 完整的使用指南，涵盖从内容创作到日常维护的全流程
date: 2026-03-01
author: Maxkore
tags: [指南, 教程, 入门]
---

# Maxkore的文档站 · 使用指南

Maxkore，你的网站已经完美运行了！这是为你定制的**完整使用指南**，涵盖从内容创作到日常维护的全流程。

---

## 📁 项目结构速览

```bash
docs-site/
├── blog/                 # 博客文章目录
│   └── 2019-05-28-hola.md
├── docs/                  # 文档目录（核心！）
│   ├── intro.md          # 入门指南
│   └── tutorial-basics/   # 教程分类文件夹
├── src/
│   ├── css/
│   │   └── custom.css     # 自定义样式
│   └── pages/             # 独立页面
│       └── index.js       # 首页（已禁用）
├── static/
│   └── img/               # 图片资源
├── docusaurus.config.ts   # 配置文件（已完美）
└── sidebars.ts            # 侧边栏配置
```

## 📝 一、写文档（核心功能）

### 1.1 创建第一个文档

在 `docs/` 目录下创建 `.md` 文件：

```markdown
---
title: 我的第一篇文章
description: 这是一篇示例文档
tags: [教程, 入门]
---

# 欢迎阅读

这是用 Markdown 写的文档内容。

## 二级标题

- 列表项1
- 列表项2

> 引用内容
```

保存后，访问：`https://docs-site.bbroot.com/文件名`

### 1.2 文档 Front Matter 详解

每个文档开头的 `---` 部分叫 Front Matter，常用字段：

| 字段 | 示例 | 作用 |
|------|------|------|
| `title` | `title: 入门指南` | 页面标题 |
| `description` | `description: 快速上手` | SEO描述 |
| `tags` | `tags: [教程, React]` | 文档标签 |
| `slug` | `slug: /guide` | 自定义URL路径 |
| `sidebar_position` | `sidebar_position: 1` | 侧边栏排序 |

### 1.3 文档组织结构

**按文件夹分类**：

```bash
docs/
├── intro.md              → /docs/intro
├── guide/
│   ├── start.md          → /docs/guide/start
│   └── advanced.md       → /docs/guide/advanced
└── api/
    └── reference.md      → /docs/api/reference
```

**添加分类说明**（在文件夹中创建 `_category_.json`）：

```json
{
  "label": "使用指南",
  "position": 2,
  "link": {
    "type": "generated-index",
    "description": "学习如何使用本工具"
  }
}
```

## 📰 二、写博客

### 2.1 创建博客文章

在 `blog/` 目录下创建文件，命名格式：`YYYY-MM-DD-标题.md`

```markdown
---
title: 我的第一篇博客
date: 2026-03-01
author: Maxkore
tags: [更新, 公告]
---

# 大家好！

这是博客内容...

<!--truncate-->  <!-- 在这之后的内容不会显示在列表页 -->

更多详细内容...
```

### 2.2 博客 Front Matter

| 字段 | 示例 | 作用 |
|------|------|------|
| `title` | `title: 版本更新` | 文章标题 |
| `date` | `date: 2026-03-01` | 发布日期 |
| `author` | `author: Maxkore` | 作者名 |
| `tags` | `tags: [更新]` | 文章标签 |
| `image` | `image: ./cover.png` | 封面图 |

访问博客列表：`https://docs-site.bbroot.com/blog`

## 🎨 三、自定义页面

在 `src/pages/` 下创建独立页面（无侧边栏）：

### React 页面示例 (`src/pages/about.tsx`)

```tsx
import React from 'react';
import Layout from '@theme/Layout';

export default function About() {
  return (
    <Layout title="关于" description="关于本站">
      <div style={{ padding: '2rem' }}>
        <h1>关于本站</h1>
        <p>这是Maxkore的个人文档站。</p>
      </div>
    </Layout>
  );
}
```

访问：`https://docs-site.bbroot.com/about`

## 🎯 四、导航与侧边栏

### 4.1 侧边栏自动生成

`sidebars.ts` 已配置为自动生成：

```typescript
module.exports = {
  tutorialSidebar: [{type: 'autogenerated', dirName: '.'}],
};
```

这意味着：
- `docs/` 下的文件夹结构自动成为侧边栏分类
- 文件按字母顺序排序（可用 `sidebar_position` 调整）

### 4.2 调整排序

在文档 Front Matter 中添加：

```markdown
---
sidebar_position: 1  # 数字越小越靠前
---
```

或在 `_category_.json` 中：

```json
{
  "position": 3  # 分类排序
}
```

## 🌗 五、主题与样式

### 5.1 主题切换

右上角 🌙/☀️ 按钮可以切换深色/浅色模式，配置已在 `docusaurus.config.ts` 中启用：

```typescript
colorMode: {
  defaultMode: 'light',
  disableSwitch: false,  // false = 允许切换
  respectPrefersColorScheme: true,  // 跟随系统
}
```

### 5.2 自定义主题色

编辑 `src/css/custom.css`：

```css
:root {
  --ifm-color-primary: #6366f1;  /* 主色调改为靛蓝色 */
  --ifm-color-primary-dark: #4f52e6;
  --ifm-color-primary-darker: #3b3fd9;
  --ifm-color-primary-light: #777af3;
}

[data-theme='dark'] {
  --ifm-color-primary: #8183f0;  /* 深色模式下的主色调 */
}
```

## 🚀 六、日常操作流程

### 6.1 本地开发

```bash
# 启动本地预览（自动热更新）
npm start

# 浏览器访问 http://localhost:3000
```

### 6.2 写内容并发布

```bash
# 1. 创建/修改文件（docs/ 或 blog/ 目录下）

# 2. 提交到 GitHub（自动部署）
git add .
git commit -m "更新文档"
git push
```

等待2-3分钟，访问 `https://docs-site.bbroot.com` 查看更新。

### 6.3 常用 Git 命令

```bash
git status        # 查看修改
git add .         # 添加所有修改
git commit -m "说明"  # 提交
git push          # 推送（自动触发部署）
```

## 🔧 七、常见问题

### Q1：修改后网站没变化？
- 检查是否 `git push` 成功
- 查看 GitHub Actions 构建状态：`https://github.com/Maxkore-Geek/docs-site/actions`
- 等待1-2分钟，CDN缓存更新

### Q2：如何删除页面？
直接删除对应的 `.md` 文件，提交即可。

### Q3：想添加搜索功能？
取消 `docusaurus.config.ts` 中 Algolia 配置的注释，并填入你的 [Algolia](https://docsearch.algolia.com/) 凭证。

### Q4：想修改网站图标？
替换 `static/img/favicon.ico` 文件。

## 📚 八、Markdown 进阶技巧

### 8.1 内嵌 React 组件（MDX）

文件后缀改为 `.mdx`，可以直接使用 React 组件：

```mdx
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 安装指南

<Tabs>
  <TabItem value="npm" label="npm" default>
    ```bash
    npm install my-package
    ```
  </TabItem>
  <TabItem value="yarn" label="yarn">
    ```bash
    yarn add my-package
    ```
  </TabItem>
</Tabs>
```

### 8.2 提示框

```markdown
:::tip 提示
这是一个小提示
:::

:::info 注意
重要信息
:::

:::warning 警告
谨慎操作！
:::

:::danger 危险
千万别这么做！
:::
```

### 8.3 代码块高亮

````markdown
```jsx title="App.js"
function App() {
  return <div>Hello</div>;
}
```